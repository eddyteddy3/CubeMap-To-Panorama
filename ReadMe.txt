Following are the steps you need to follow in order to have it work for you.

1. Create the virtual environment

```sh

python3 -m venv venv

```

(make sure you have python3 installed in your system)

2. Activate Venv

```sh

source venv/bin/activate

```

3. Install Requirements

```sh
pip install -r requirements.txt
```

4. Add images to `image_set`

(the code still need to be refined because right now, its following the static a heirarchy with at most 3 levels)